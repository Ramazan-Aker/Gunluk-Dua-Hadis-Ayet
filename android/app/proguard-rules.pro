# App-specific R8 rules belong in this file. Flutter, Firebase, Google Play
# services and the bundled Android plugins provide their own consumer rules.
# Keep this file intentionally minimal so release code can be fully optimized.
